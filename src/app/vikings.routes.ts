import { Routes } from '@angular/router';
import { VikingsComponent } from './vikings/vikings';

export const VIKINGS_ROUTES: Routes = [
    {
        path: 'vikings',
        component: VikingsComponent,
        title: 'Vikings',
    },
];
